syntax = "proto3";

package ddpoll;

service DDPoll {
    rpc Authenticate(AuthQuery) returns (AuthResp) {}
}

message AuthQuery {
    string name = 1;
    string password = 2;
}

message AuthResp {
    int32 status = 1;
    string sessionKey = 2;
}